fedora:
  dependencies: [dotfiles.yml, sddm.ym, polybar.yml, rofi.yml, fonts.yml]
  install:
    packages: bspwm

  post-install:
    command: |+
      (cd $HOME/.dotfiles && stow bspwm)
      (cd $HOME/.dotfiles && stow sxhkd)
